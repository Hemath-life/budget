1. packages/ui/
   ├── pnpm build → tsc (generates .d.ts files in dist/)
   ├── NO JavaScript bundling
   └── Source files stay in src/

2. apps/kasu/
   ├── pnpm build → tsc -b && vite build
   ├── Imports from uix (source files)
   ├── Vite bundles UI components WITH app code
   └── Tree-shaking removes unused components