/**
 * Utility Functions
 *
 * @description
 * Core utility functions for the UI library.
 *
 * @example
 * import { cn } from '@repo/ui/lib';
 *
 * // Merge class names with conditional logic
 * <div className={cn('base-class', isActive && 'active-class', className)} />
 */

export { cn } from './utils';
